# Type of Admission and Origin {#origin .tabset .tabset-fade .tabset-pills}

## Overview {-}

The system allow to distinguish the first from subsequent admissions in the same unit. Admission was coded as mode, type, origin, and reasons. All the metrics can be visualized stratified by gender and age class.

**Mode**:

- planned (defined as admission that might be postponed for 24 hours)
- unplanned
- re-admission


**Type**:

- medical
- surgical
- trauma

**Origin**:

- ward
- emergency room
- operating room
- other hospital
- home
- other origin


> number of admission for the same patient beside the first



## Mode  {.tabset .tabset-dropdown}

### Planned and Unplanned
```{r, eval=!params$export}
originReportUI("m_planned")
```

```{r, context = "server", eval=!params$export}
originReport(
  "m_planned",
  data = data_ranged[["full_filtered"]],
  what = "ricovero_progr"
)
```

```{r, context = "server", eval=params$export, fig.height=11}
originReportStatic(
  data = data_ranged[["full_filtered"]](),
  completed = params[["d_gender_completed"]],
  what = "ricovero_progr"
)
```

### Re-admissions

```{r, eval=!params$export}
originReportUI("m_admission")
```

```{r, context = "server", eval=!params$export}
originReport(
  "m_admission",
  data = data_ranged[["full_filtered"]],
  what = "redcap_repeat_instance"
)
```

```{r, context = "server", eval=params$export, fig.height=11}
originReportStatic(
  data = data_ranged[["full_filtered"]](),
  completed = params[["d_gender_completed"]],
  what = "redcap_repeat_instance"
)
```

## Origin and type {.tabset .tabset-dropdown}

### Overview
```{r, eval=!params$export}
originReportUI("m_provenienza")
```

```{r, context = "server", eval=!params$export}
originReport(
  "m_provenienza",
  data = data_ranged[["full_filtered"]],
  what = "provenienza"
)
```

```{r, context = "server", eval=params$export, fig.height=11}
originReportStatic(
  data = data_ranged[["full_filtered"]](),
  completed = params[["d_gender_completed"]],
  what = "provenienza"
)
```
### Other hospital
```{r, eval=!params$export}
originReportUI("m_altro_osp")
```

```{r, context = "server", eval=!params$export}
originReport(
  "m_altro_osp",
  data = data_ranged[["full_filtered"]],
  what = "altro_osp"
)
```

```{r, context = "server", eval=params$export, fig.height=11}
originReportStatic(
  data = data_ranged[["full_filtered"]](),
  completed = params[["d_gender_completed"]],
  what = "altro_osp"
)
```
### Type
```{r, eval=!params$export}
originReportUI("m_tipologia")
```

```{r, context = "server", eval=!params$export}
originReport(
  "m_tipologia",
  data = data_ranged[["full_filtered"]],
  what = "tipologia2"
)
```

```{r, context = "server", eval=params$export, fig.height=11}
originReportStatic(
  data = data_ranged[["full_filtered"]](),
  completed = params[["d_gender_completed"]],
  what = "tipologia2"
)
```
### Type of surgery
```{r, eval=!params$export}
originReportUI("m_tipo_chir")
```

```{r, context = "server", eval=!params$export}
originReport(
  "m_tipo_chir",
  data = data_ranged[["full_filtered"]],
  what = "tipo_chir"
)
```

```{r, context = "server", eval=params$export, fig.height=11}
originReportStatic(
  data = data_ranged[["full_filtered"]](),
  completed = params[["d_gender_completed"]],
  what = "tipo_chir"
)
```
## Priority level of admission  {.tabset .tabset-dropdown}
```{r, eval=!params$export}
originReportUI("m_priorita")
```

```{r, context = "server", eval=!params$export}
originReport(
  "m_priorita",
  data = data_ranged[["full_filtered"]],
  what = "priorita"
)
```

```{r, context = "server", eval=params$export, fig.height=11}
originReportStatic(
  data = data_ranged[["full_filtered"]](),
  completed = params[["d_gender_completed"]],
  what = "priorita"
)
```
## Reasons {.tabset .tabset-dropdown}

### Post-operative PICUs admissions

### Medical PICUs admissions

### Trauma

### brain trauma

### PICUs admission after injuries
