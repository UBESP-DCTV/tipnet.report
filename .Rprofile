source("renv/activate.R")


# if (interactive()) {
#   suppressPackageStartupMessages(suppressWarnings({
#     library(usethis)
#     library(devtools)
#     library(testthat)
#   }))
# }
#
#
if (interactive()) {
  options(tipnet.dev = TRUE)
}
